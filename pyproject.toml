[project]
name = "minimind-twin"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "劉昕晟 LIU XINSHENG", email = "103816369+Babe153@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.3.4",
    "pandas>=2.3.3",
    "torch>=2.9.0",
    "transformers>=4.57.1",
]

[project.scripts]
minimind-twin = "minimind_twin:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
